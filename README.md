# ⚡ spark-fraud-detection

**Description:** Real-time large-scale anomaly and fraud detection model using PySpark.

## 🌟 Project Overview

โปรเจกต์นี้มีวัตถุประสงค์เพื่อสร้างระบบตรวจจับการฉ้อโกง (Fraud Detection System) ในธุรกรรมทางการเงิน โดยเน้นการประมวลผลข้อมูลขนาดใหญ่ (Big Data) เพื่อจัดการกับปัญหาความไม่สมดุลของข้อมูล (Class Imbalance) ที่รุนแรง

มีการทดลองสร้างและเปรียบเทียบโมเดลด้วย 2 แนวทางหลัก คือ:
1.  **PySpark Pipeline:** ใช้ชุดข้อมูลเต็ม (6.35 ล้านแถว) พร้อมเทคนิค **Class Weighting**
2.  **Scikit-learn:** ใช้เทคนิค **Random Under-sampling** เพื่อปรับสมดุลข้อมูลก่อนสร้างโมเดล

## 🛠️ เทคโนโลยีและกระบวนการหลัก

* **ชุดข้อมูล:** Transaction Dataset (6,351,193 แถว, 10 คอลัมน์) 
* **โมเดล:** Random Forest Classifier 
* **การจัดการ Imbalance:** Class Weighting และ Random Under-sampling 

### 1. PySpark Machine Learning Pipeline

Pipeline ที่สร้างขึ้นสำหรับ PySpark มีทั้งหมด 5 Stages
1.  **StringIndexer:** แปลงคอลัมน์ `type` (Categorical) เป็นตัวเลข 
2.  **OneHotEncoder:** เข้ารหัส `type_index`
3.  **VectorAssembler:** รวมคุณลักษณะทั้งหมดเข้าด้วยกัน 
4.  **PCA:** ลดมิติข้อมูล (k=5) 
5.  **RandomForestClassifier:** โมเดลทำนาย (ตั้งค่า `weightCol="classWeight"`) 

---

## 📊 ผลลัพธ์และการเปรียบเทียบประสิทธิภาพโมเดล

ชุดข้อมูลมีอัตราส่วนความไม่สมดุล (**Normal:Fraud = 822.01:1**)

| แนวทางโมเดล | AUC-ROC | AUC-PR | Precision | Recall | F1-Score | ข้อมูลที่ใช้ฝึก |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1. PySpark (Class Weight)** | **0.9938**  | t]0.6336 | 2.84% (0.0284) | 95.79% (0.9579) | 97.82% (0.9782) | 4.4 ล้านแถว  |
| **2. Scikit-learn (Undersampling)** | N/A | N/A | **98.78%** (0.9878)  | **99.81%** (0.9981) | **99.29%** (0.9929)  | 15K แถว |

### 🏆 สรุปผลการประเมิน

| Metric | แนวทางที่ชนะ | ข้อสังเกต |
| :--- | :--- | :--- |
| **AUC-ROC / AUC-PR** | **PySpark** | เหมาะสำหรับการวัดประสิทธิภาพโดยรวมในการจัดอันดับ (Ranking) |
| **Precision, Recall, F1-Score** | **Scikit-learn** | โมเดลที่ใช้ Undersampling มีความสามารถในการจำแนกสูงมาก *บนชุดข้อมูลที่สมดุลแล้ว* |

### 🔍 การวิเคราะห์เชิงลึก (Confusion Matrix)

#### **1. PySpark (Class Weight) - Confusion Matrix**
| Actual Label \ Predicted Label | Predicted Normal | Predicted Fraud |
| :--- | :--- | :--- |
| **Actual Normal** (`isFraud=0`) |1,826,399 | 76,866 |
| **Actual Fraud** (`isFraud=1`) | 99 | 2,250 |

* **False Positives (FP):** 76,866 (โมเดลทำนายผิดพลาดว่าเป็น Fraud จำนวนมาก)
* **False Negatives (FN):** 99 (พลาดการตรวจจับ Fraud จริงไป 99 ราย)
* *ข้อสังเกต:* Recall สูงมาก (95.79%) เนื่องจากโมเดลยอมแลกด้วย Precision ที่ต่ำมาก (2.84%) และ FP ที่สูงมาก เพื่อให้พลาด Fraud จริงน้อยที่สุด (FN ต่ำ)

#### **2. Scikit-learn (Undersampling) - Confusion Matrix**
| Actual Label \ Predicted Label | Predicted Normal | Predicted Fraud |
| :--- | :--- | :--- |
| **Actual Normal** (`isFraud=0`) |1,525 | 19 |
| **Actual Fraud** (`isFraud=1`) | 3 | 1,540 |

* **False Positives (FP):** 19 
* **False Negatives (FN):** 3 
* *ข้อสังเกต:* หลังการ Undersampling โมเดลมี **FP และ FN ที่ต่ำมาก** และได้ **Precision (98.78%)** และ **Recall (99.81%)** สูงมากบนชุดข้อมูลที่สมดุลแล้ว

### 🚀 ข้อสรุปสุดท้าย

1.  **PySpark (Class Weighting):** เหมาะสำหรับการทำงานกับ **ข้อมูลขนาดใหญ่** โดยตรง แต่ให้ค่า **Precision ต่ำมาก** (FP สูง) ซึ่งหมายถึงการแจ้งเตือนผิดพลาดจำนวนมาก
2.  Scikit-learn (Undersampling):** ให้ผลลัพธ์ **Precision, Recall, F1-Score** สูงกว่ามาก และมี FP ต่ำกว่าอย่างเห็นได้ชัด

**การเลือกโมเดล:** หากเป้าหมายคือ **ความแม่นยำสูงที่สุดในการจำแนก** และยอมรับการลดขนาดข้อมูล (และผลที่ตามมาคือการสูญเสียข้อมูลบางส่วน) **แนวทาง Scikit-learn + Undersampling** จะให้โมเดลที่ใช้งานได้จริงมากกว่า เนื่องจากมีอัตราการแจ้งเตือนผิดพลาด (FP) ต่ำกว่ามาก

---
